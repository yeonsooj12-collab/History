{
  "name": "history-lens-chatgpt-app",
  "version": "0.5.2",
  "private": true,
  "type": "module",
  "description": "세계사 조건 렌즈 ChatGPT App and MCP server",
  "scripts": {
    "build": "node scripts/build-widget.mjs",
    "start": "npm run build && node server.js",
    "check": "node --check server.js && node --check scripts/build-widget.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.1",
    "@modelcontextprotocol/sdk": "^1.20.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=20"
  }
}
